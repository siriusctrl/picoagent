Wait for selected background tasks for the configured bounded interval and report their current states. Pass task_ids=[] to wait for all tasks created by this run. The interval is fixed by the harness, does not cancel tasks, and is shorter than the enclosing tool-call timeout; call wait again if needed. A terminal result is delivered separately as one durable background-result message, so waiting never consumes or duplicates it.
